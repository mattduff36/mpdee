# NPM configuration for MPDEE

# Save exact versions for reproducibility
save-exact=true

# Faster installs
prefer-offline=false

# Audit settings
audit-level=high

# Generate package-lock.json
package-lock=true
